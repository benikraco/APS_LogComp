
# Definição de função
funcao soma(x::Int, y::Int)::Int
    retorna x + y
fim

# Definição (com tipagem) de variável
x_1::Int
x_1 = 2
x_1 = soma(1, x_1)

x_1 = leia()

se ((x_1 > 1) && !(x_1 < 1)) 
    x_1 = 3
senao 
    
    x_1 = (-20+30)*4*3/40 # teste de comentario
    
fim
imprima(x_1)
x_1 = leia()
se ((x_1 > 1) && !(x_1 < 1))
    x_1 = 3
senao
    x_1 = (-20+30)*12/40
fim
imprima(x_1)
enquanto ((x_1 > 1) || (x_1 == 1)) 
    x_1 = x_1 - 1
    imprima(x_1)
fim